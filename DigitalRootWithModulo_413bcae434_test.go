// Test generated by RoostGPT for test roost-test using AI Type Open AI and AI Model gpt-4

package main

import (
	"testing"
)

func TestDigitalRootWithModulo_413bcae434(t *testing.T) {
	var tests = []struct {
		input    int
		expected int
	}{
		{12345, 6},
		{9999, 9},
		{0, 0},
		{1111, 4},
		{-9999, -9},
	}

	for _, test := range tests {
		if output := DigitalRootWithModulo(test.input); output != test.expected {
			t.Error("Test failed: {} inputted, {} expected, recieved: {}", test.input, test.expected, output)
		}
	}
}

func DigitalRootWithModulo(number int) int {
	if number < 0 {
		return -DigitalRootWithModulo(-number)
	}

	if (10 - number > 0) {
		return number
	}

	var sum, remainder int
	for number != 0 {
		remainder = number % 10
		sum = sum + remainder
		number = number / 10
	}

	return DigitalRootWithModulo(sum)
}
